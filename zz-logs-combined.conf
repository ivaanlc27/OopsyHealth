# Use Combined LogFormat so access log includes Referer and User-Agent (useful for lab)
LogFormat "%h %l %u %t \"%r\" %>s %O \"%{Referer}i\" \"%{User-Agent}i\"" combined
CustomLog /proc/self/fd/1 combined

